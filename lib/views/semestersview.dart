import 'package:flutter/material.dart';
import 'components.dart';
import '../controllers/semesterscontroller.dart';

class SemesterPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
      body: SafeArea(
        child: _renderBody(),
      ),
    );
  }

  Widget _renderBody() {
    // var body ListWidget;

  }
}